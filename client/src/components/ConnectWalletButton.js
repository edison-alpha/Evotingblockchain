// import React, { useContext } from "react";
// import { AuthContext } from "./AuthContext";

// const ConnectWalletButton = () => {
//     const { connected, account, connectAccount, disconnectAccount } = useContext(AuthContext);

//     return (
//         <div>
//             {!connected ? (
//                 <button onClick={connectAccount}>Connect Wallet</button>
//             ) : (
//                 <div>
//                     <p>Connected wallet address: {account}</p>
//                     <button onClick={disconnectAccount}>Disconnect Wallet</button>
//                 </div>
//             )}
//         </div>
//     );
// };

// export default ConnectWalletButton;
